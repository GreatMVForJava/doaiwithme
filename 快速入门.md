# Vibe Coding 快速入门

> 5 分钟学会 AI 协作编程

---

## 一、两个目录

| 目录 | 干什么的 | 你需要做什么 |
|------|---------|------------|
| `.iflow/` | AI 协作配置 | 告诉 AI 遵循这些规范 |
| `app-demand/` | 业务需求管理 | 写需求文档，提问题 |

---

## 二、怎么用

### 给 AI 发任务时

复制这句，附在需求后面：

```
依照 @.iflow/start.md 执行
```

### 任务完成后

让 AI 读 endPoint.md 自动收尾：

```
依照 @.iflow/endPoint.md 继续执行
```

endPoint.md 能让 AI：
- 自动分析当前需求状态
- 自动恢复上下文继续开发
- 自动迭代直到完成

### 写需求文档时

1. 在 `app-demand/` 下创建日期文件夹，如 `2026-02-26/`
2. 创建需求文件夹，如 `报告解读/`
3. 写一个 `需求文档.md`

> **大需求才需要冷启动**：如果需求背景复杂、涉及多个模块，让 AI 根据 `@APP_DEMAND_SPEC.md` 生成 `需求冷启动.md`，帮 AI 理解上下文。

---

## 三、AI 会怎么做

AI 收到任务后会按这个流程执行：

```
确认 → 规划 → 执行 → 自检 → 沉淀
```

你需要做的：
1. **确认阶段**：检查 AI 是否理解正确
2. **规划阶段**：确认执行计划
3. **执行阶段**：等待执行，给出反馈
4. **沉淀阶段**：让 AI 把经验写下来

---

## 四、记住这几点

1. **别重复造轮子** - 先问 AI 有没有现成的代码可以复用
2. **做完要记录** - 好的做法让 AI 写下来，下次遇到同样问题直接用
3. **搞不定就跳过** - 不是所有问题都要解决，先解决能解决的
4. **规范不用全遵守** - 按需选择，够用就行

---

## 五、常见问题

**Q: AI 总是问重复的问题？**
A: 在 `需求冷启动.md` 里写清楚背景，AI 会先读这个。

**Q: AI 写的代码不符合规范？**
A: 检查需求文档是否清晰，确认阶段让 AI 复述理解。

**Q: 怎么让 AI 记住经验？**
A: 执行完成后，让 AI 更新 `需求冷启动.md` 或 `.iflow/context/experience/`。

**Q: AI 不遵守规则怎么办？**
A: 参考以下方法：

| 方法 | 说明 |
|------|------|
| 提醒AI | "按照 CHECKPOINT_SPEC.md 的检查点执行" |
| 明确拒绝 | "不允许，重新执行" |
| 回滚重来 | "回滚到上一个正确状态" |

**预防措施**：每次任务开始时发送：
```
依照 @.iflow/start.md 执行，遵守关键操作检查点
```

---

## 六、详细文档

想深入了解？看这些：

- [start.md](.iflow/start.md) - AI 行为规范
- [GLOBAL_CONCEPTS.md](.iflow/context/concept/GLOBAL_CONCEPTS.md) - 核心概念
- [requirementsGuidelines.md](app-demand/requirementsGuidelines.md) - 需求编写指南
